// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Manages Autoscaling Groups.
 *
 * ## Example Usage
 *
 * ### CPU Target
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * const main = new scaleway.instance.Template("main", {serverType: "GP1-L"});
 * const mainGroup = new scaleway.autoscaling.Group("main", {
 *     templateId: main.id,
 *     tags: [
 *         "tf-examples",
 *         "scaleway_autoscaling_group",
 *         "cpu-target",
 *     ],
 *     name: "asg-cpu-target",
 *     scalingPolicy: {
 *         minimumSize: 0,
 *         maximumSize: 25,
 *         scaleInCooldown: "2m",
 *         scaleOutCooldown: "1m",
 *         scaleInStep: 5,
 *         scaleOutStep: 5,
 *         cpuTarget: 92,
 *     },
 * });
 * ```
 *
 * ### Fixed size
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * const main = new scaleway.instance.Template("main", {serverType: "PRO2-M"});
 * const mainGroup = new scaleway.autoscaling.Group("main", {
 *     templateId: main.id,
 *     tags: [
 *         "tf-examples",
 *         "scaleway_autoscaling_group",
 *         "fixed-size",
 *     ],
 *     name: "asg-fixed-size",
 *     scalingPolicy: {
 *         minimumSize: 1,
 *         maximumSize: 20,
 *         scaleInCooldown: "7m",
 *         scaleOutCooldown: "30s",
 *         fixedSize: 10,
 *     },
 * });
 * ```
 *
 * ### Load Balancer Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // VPC resources
 * const vpc = new scaleway.network.Vpc("vpc", {});
 * const pn = new scaleway.network.PrivateNetwork("pn", {vpcId: vpc.id});
 * // LB resources
 * const lb = new scaleway.loadbalancers.LoadBalancer("lb", {type: "LB-S"});
 * const back0 = new scaleway.loadbalancers.Backend("back0", {
 *     lbId: lb.id,
 *     forwardProtocol: "http",
 *     forwardPort: 80,
 * });
 * const back1 = new scaleway.loadbalancers.Backend("back1", {
 *     lbId: lb.id,
 *     forwardProtocol: "tcp",
 *     forwardPort: 81,
 * });
 * // Template
 * const main = new scaleway.instance.Template("main", {
 *     tags: [
 *         "terraform-test",
 *         "scaleway_autoscaling_group",
 *         "lb_config",
 *     ],
 *     serverType: "PRO2-M",
 * });
 * // AutoScaling Group
 * const mainGroup = new scaleway.autoscaling.Group("main", {
 *     templateId: main.id,
 *     scalingPolicy: {
 *         minimumSize: 0,
 *         maximumSize: 25,
 *         memoryTarget: 75,
 *     },
 *     loadBalancerConfiguration: {
 *         loadBalancerId: lb.id,
 *         backends: [
 *             {
 *                 backendId: back0.id,
 *                 addressFamily: "ipv6",
 *             },
 *             {
 *                 backendId: back1.id,
 *                 addressFamily: "ipv4",
 *                 privateNetworkId: pn.id,
 *             },
 *         ],
 *         autoHealing: {
 *             enabled: true,
 *             gracePeriod: "10m",
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * AutoScaling Groups can be imported using their `id`:
 *
 * ```sh
 * $ pulumi import scaleway:autoscaling/group:Group main <group_id>
 * ```
 */
export class Group extends pulumi.CustomResource {
    /**
     * Get an existing Group resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GroupState, opts?: pulumi.CustomResourceOptions): Group {
        return new Group(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'scaleway:autoscaling/group:Group';

    /**
     * Returns true if the given object is an instance of Group.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Group {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Group.__pulumiType;
    }

    /**
     * The creation timestamp of the AutoScaling Group.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The load balancer configuration.
     *
     * > **Important:** Due to current limitations, this block cannot be unset. To remove it once it has been set, the resource needs to be recreated.
     *
     * > The `loadBalancerConfiguration` block contains:
     */
    declare public readonly loadBalancerConfiguration: pulumi.Output<outputs.autoscaling.GroupLoadBalancerConfiguration>;
    /**
     * The name of the AutoScaling Group. If not set, a name will be generated.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The ID of the project the AutoScaling Group is associated with.
     */
    declare public readonly projectId: pulumi.Output<string>;
    /**
     * The scaling policy configuration.
     *
     * > The `scalingPolicy` block contains:
     */
    declare public readonly scalingPolicy: pulumi.Output<outputs.autoscaling.GroupScalingPolicy>;
    /**
     * The current status of the AutoScaling Group.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * The tags associated with the AutoScaling Group.
     */
    declare public readonly tags: pulumi.Output<string[] | undefined>;
    /**
     * The ID of the Instance Template used to create instances in this group.
     */
    declare public readonly templateId: pulumi.Output<string>;
    /**
     * The last update timestamp of the AutoScaling Group.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;
    /**
     * The zone in which the AutoScaling Group should be created.
     */
    declare public readonly zone: pulumi.Output<string>;

    /**
     * Create a Group resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: GroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GroupArgs | GroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GroupState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["loadBalancerConfiguration"] = state?.loadBalancerConfiguration;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["scalingPolicy"] = state?.scalingPolicy;
            resourceInputs["status"] = state?.status;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["templateId"] = state?.templateId;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["zone"] = state?.zone;
        } else {
            const args = argsOrState as GroupArgs | undefined;
            if (args?.scalingPolicy === undefined && !opts.urn) {
                throw new Error("Missing required property 'scalingPolicy'");
            }
            if (args?.templateId === undefined && !opts.urn) {
                throw new Error("Missing required property 'templateId'");
            }
            resourceInputs["loadBalancerConfiguration"] = args?.loadBalancerConfiguration;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["scalingPolicy"] = args?.scalingPolicy;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["templateId"] = args?.templateId;
            resourceInputs["zone"] = args?.zone;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Group.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Group resources.
 */
export interface GroupState {
    /**
     * The creation timestamp of the AutoScaling Group.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * The load balancer configuration.
     *
     * > **Important:** Due to current limitations, this block cannot be unset. To remove it once it has been set, the resource needs to be recreated.
     *
     * > The `loadBalancerConfiguration` block contains:
     */
    loadBalancerConfiguration?: pulumi.Input<inputs.autoscaling.GroupLoadBalancerConfiguration | undefined>;
    /**
     * The name of the AutoScaling Group. If not set, a name will be generated.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project the AutoScaling Group is associated with.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * The scaling policy configuration.
     *
     * > The `scalingPolicy` block contains:
     */
    scalingPolicy?: pulumi.Input<inputs.autoscaling.GroupScalingPolicy | undefined>;
    /**
     * The current status of the AutoScaling Group.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * The tags associated with the AutoScaling Group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The ID of the Instance Template used to create instances in this group.
     */
    templateId?: pulumi.Input<string | undefined>;
    /**
     * The last update timestamp of the AutoScaling Group.
     */
    updatedAt?: pulumi.Input<string | undefined>;
    /**
     * The zone in which the AutoScaling Group should be created.
     */
    zone?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Group resource.
 */
export interface GroupArgs {
    /**
     * The load balancer configuration.
     *
     * > **Important:** Due to current limitations, this block cannot be unset. To remove it once it has been set, the resource needs to be recreated.
     *
     * > The `loadBalancerConfiguration` block contains:
     */
    loadBalancerConfiguration?: pulumi.Input<inputs.autoscaling.GroupLoadBalancerConfiguration | undefined>;
    /**
     * The name of the AutoScaling Group. If not set, a name will be generated.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID of the project the AutoScaling Group is associated with.
     */
    projectId?: pulumi.Input<string | undefined>;
    /**
     * The scaling policy configuration.
     *
     * > The `scalingPolicy` block contains:
     */
    scalingPolicy: pulumi.Input<inputs.autoscaling.GroupScalingPolicy>;
    /**
     * The tags associated with the AutoScaling Group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The ID of the Instance Template used to create instances in this group.
     */
    templateId: pulumi.Input<string>;
    /**
     * The zone in which the AutoScaling Group should be created.
     */
    zone?: pulumi.Input<string | undefined>;
}
