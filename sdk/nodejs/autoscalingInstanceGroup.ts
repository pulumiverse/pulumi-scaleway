// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Books and manages Autoscaling Instance groups.
 *
 * ## Example Usage
 *
 * ### Basic
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * const main = new scaleway.AutoscalingInstanceGroup("main", {
 *     name: "asg-group",
 *     templateId: mainScalewayAutoscalingInstanceTemplate.id,
 *     tags: [
 *         "terraform-test",
 *         "instance-group",
 *     ],
 *     capacities: [{
 *         maxReplicas: 5,
 *         minReplicas: 1,
 *         cooldownDelay: 300,
 *     }],
 *     loadBalancers: [{
 *         id: mainScalewayLb.id,
 *         backendIds: [mainScalewayLbBackend.id],
 *         privateNetworkId: mainScalewayVpcPrivateNetwork.id,
 *     }],
 * });
 * ```
 *
 * ### With template and policies
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * const main = new scaleway.network.Vpc("main", {name: "TestAccAutoscalingVPC"});
 * const mainPrivateNetwork = new scaleway.network.PrivateNetwork("main", {
 *     name: "TestAccAutoscalingVPC",
 *     vpcId: main.id,
 * });
 * const mainVolume = new scaleway.block.Volume("main", {
 *     iops: 5000,
 *     sizeInGb: 10,
 * });
 * const mainSnapshot = new scaleway.block.Snapshot("main", {
 *     name: "test-ds-block-snapshot-basic",
 *     volumeId: mainVolume.id,
 * });
 * const mainIp = new scaleway.loadbalancers.Ip("main", {});
 * const mainLoadBalancer = new scaleway.loadbalancers.LoadBalancer("main", {
 *     ipId: mainIp.id,
 *     name: "test-lb",
 *     type: "lb-s",
 *     privateNetworks: [{
 *         privateNetworkId: mainPrivateNetwork.id,
 *     }],
 * });
 * const mainBackend = new scaleway.loadbalancers.Backend("main", {
 *     lbId: mainLoadBalancer.id,
 *     forwardProtocol: "tcp",
 *     forwardPort: 80,
 *     proxyProtocol: "none",
 * });
 * const mainAutoscalingInstanceTemplate = new scaleway.AutoscalingInstanceTemplate("main", {
 *     name: "autoscaling-instance-template-basic",
 *     commercialType: "PLAY2-MICRO",
 *     tags: [
 *         "terraform-test",
 *         "basic",
 *     ],
 *     volumes: [{
 *         name: "as-volume",
 *         volumeType: "sbs",
 *         boot: true,
 *         fromSnapshot: {
 *             snapshotId: mainSnapshot.id,
 *         },
 *         perfIops: 5000,
 *     }],
 *     publicIpsV4Count: 1,
 *     privateNetworkIds: [mainPrivateNetwork.id],
 * });
 * const mainAutoscalingInstanceGroup = new scaleway.AutoscalingInstanceGroup("main", {
 *     name: "autoscaling-instance-group-basic",
 *     templateId: mainAutoscalingInstanceTemplate.id,
 *     tags: [
 *         "terraform-test",
 *         "instance-group",
 *     ],
 *     capacities: [{
 *         maxReplicas: 5,
 *         minReplicas: 1,
 *         cooldownDelay: 300,
 *     }],
 *     loadBalancers: [{
 *         id: mainLoadBalancer.id,
 *         backendIds: [mainBackend.id],
 *         privateNetworkId: mainPrivateNetwork.id,
 *     }],
 *     deleteServersOnDestroy: true,
 * });
 * const up = new scaleway.AutoscalingInstancePolicy("up", {
 *     instanceGroupId: mainAutoscalingInstanceGroup.id,
 *     name: "scale-up-if-cpu-high",
 *     action: "scale_up",
 *     type: "flat_count",
 *     value: 1,
 *     priority: 1,
 *     metrics: [{
 *         name: "cpu scale up",
 *         managedMetric: "managed_metric_instance_cpu",
 *         operator: "operator_greater_than",
 *         aggregate: "aggregate_average",
 *         samplingRangeMin: 5,
 *         threshold: 70,
 *     }],
 * });
 * const down = new scaleway.AutoscalingInstancePolicy("down", {
 *     instanceGroupId: mainAutoscalingInstanceGroup.id,
 *     name: "scale-down-if-cpu-low",
 *     action: "scale_down",
 *     type: "flat_count",
 *     value: 1,
 *     priority: 2,
 *     metrics: [{
 *         name: "cpu scale down",
 *         managedMetric: "managed_metric_instance_cpu",
 *         operator: "operator_less_than",
 *         aggregate: "aggregate_average",
 *         samplingRangeMin: 5,
 *         threshold: 40,
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * Autoscaling Instance groups can be imported using `{zone}/{id}`, e.g.
 *
 * bash
 *
 * ```sh
 * $ pulumi import scaleway:index/autoscalingInstanceGroup:AutoscalingInstanceGroup main fr-par-1/11111111-1111-1111-1111-111111111111
 * ```
 */
export class AutoscalingInstanceGroup extends pulumi.CustomResource {
    /**
     * Get an existing AutoscalingInstanceGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AutoscalingInstanceGroupState, opts?: pulumi.CustomResourceOptions): AutoscalingInstanceGroup {
        return new AutoscalingInstanceGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'scaleway:index/autoscalingInstanceGroup:AutoscalingInstanceGroup';

    /**
     * Returns true if the given object is an instance of AutoscalingInstanceGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AutoscalingInstanceGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AutoscalingInstanceGroup.__pulumiType;
    }

    /**
     * The specification of the minimum and maximum replicas for the Instance group, and the cooldown interval between two scaling events.
     */
    public readonly capacities!: pulumi.Output<outputs.AutoscalingInstanceGroupCapacity[]>;
    /**
     * Date and time of Instance group's creation (RFC 3339 format).
     */
    public /*out*/ readonly createdAt!: pulumi.Output<string>;
    /**
     * Whether to delete all instances in this group when the group is destroyed. Set to `true` to tear them down, `false` (the default) leaves them running.
     */
    public readonly deleteServersOnDestroy!: pulumi.Output<boolean | undefined>;
    /**
     * The specification of the Load Balancer to link to the Instance group.
     */
    public readonly loadBalancers!: pulumi.Output<outputs.AutoscalingInstanceGroupLoadBalancer[]>;
    /**
     * The Instance group name.
     */
    public readonly name!: pulumi.Output<string>;
    /**
     * `projectId`) The ID of the Project the Instance group is associated with.
     */
    public readonly projectId!: pulumi.Output<string>;
    /**
     * The tags associated with the Instance group.
     */
    public readonly tags!: pulumi.Output<string[] | undefined>;
    /**
     * The ID of the Instance template to attach to the Instance group.
     */
    public readonly templateId!: pulumi.Output<string>;
    /**
     * Date and time of Instance group's last update (RFC 3339 format).
     */
    public /*out*/ readonly updatedAt!: pulumi.Output<string>;
    /**
     * `zone`) The zone in which the Instance group exists.
     */
    public readonly zone!: pulumi.Output<string>;

    /**
     * Create a AutoscalingInstanceGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AutoscalingInstanceGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AutoscalingInstanceGroupArgs | AutoscalingInstanceGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AutoscalingInstanceGroupState | undefined;
            resourceInputs["capacities"] = state ? state.capacities : undefined;
            resourceInputs["createdAt"] = state ? state.createdAt : undefined;
            resourceInputs["deleteServersOnDestroy"] = state ? state.deleteServersOnDestroy : undefined;
            resourceInputs["loadBalancers"] = state ? state.loadBalancers : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["projectId"] = state ? state.projectId : undefined;
            resourceInputs["tags"] = state ? state.tags : undefined;
            resourceInputs["templateId"] = state ? state.templateId : undefined;
            resourceInputs["updatedAt"] = state ? state.updatedAt : undefined;
            resourceInputs["zone"] = state ? state.zone : undefined;
        } else {
            const args = argsOrState as AutoscalingInstanceGroupArgs | undefined;
            if ((!args || args.templateId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'templateId'");
            }
            resourceInputs["capacities"] = args ? args.capacities : undefined;
            resourceInputs["deleteServersOnDestroy"] = args ? args.deleteServersOnDestroy : undefined;
            resourceInputs["loadBalancers"] = args ? args.loadBalancers : undefined;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["projectId"] = args ? args.projectId : undefined;
            resourceInputs["tags"] = args ? args.tags : undefined;
            resourceInputs["templateId"] = args ? args.templateId : undefined;
            resourceInputs["zone"] = args ? args.zone : undefined;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AutoscalingInstanceGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AutoscalingInstanceGroup resources.
 */
export interface AutoscalingInstanceGroupState {
    /**
     * The specification of the minimum and maximum replicas for the Instance group, and the cooldown interval between two scaling events.
     */
    capacities?: pulumi.Input<pulumi.Input<inputs.AutoscalingInstanceGroupCapacity>[]>;
    /**
     * Date and time of Instance group's creation (RFC 3339 format).
     */
    createdAt?: pulumi.Input<string>;
    /**
     * Whether to delete all instances in this group when the group is destroyed. Set to `true` to tear them down, `false` (the default) leaves them running.
     */
    deleteServersOnDestroy?: pulumi.Input<boolean>;
    /**
     * The specification of the Load Balancer to link to the Instance group.
     */
    loadBalancers?: pulumi.Input<pulumi.Input<inputs.AutoscalingInstanceGroupLoadBalancer>[]>;
    /**
     * The Instance group name.
     */
    name?: pulumi.Input<string>;
    /**
     * `projectId`) The ID of the Project the Instance group is associated with.
     */
    projectId?: pulumi.Input<string>;
    /**
     * The tags associated with the Instance group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The ID of the Instance template to attach to the Instance group.
     */
    templateId?: pulumi.Input<string>;
    /**
     * Date and time of Instance group's last update (RFC 3339 format).
     */
    updatedAt?: pulumi.Input<string>;
    /**
     * `zone`) The zone in which the Instance group exists.
     */
    zone?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AutoscalingInstanceGroup resource.
 */
export interface AutoscalingInstanceGroupArgs {
    /**
     * The specification of the minimum and maximum replicas for the Instance group, and the cooldown interval between two scaling events.
     */
    capacities?: pulumi.Input<pulumi.Input<inputs.AutoscalingInstanceGroupCapacity>[]>;
    /**
     * Whether to delete all instances in this group when the group is destroyed. Set to `true` to tear them down, `false` (the default) leaves them running.
     */
    deleteServersOnDestroy?: pulumi.Input<boolean>;
    /**
     * The specification of the Load Balancer to link to the Instance group.
     */
    loadBalancers?: pulumi.Input<pulumi.Input<inputs.AutoscalingInstanceGroupLoadBalancer>[]>;
    /**
     * The Instance group name.
     */
    name?: pulumi.Input<string>;
    /**
     * `projectId`) The ID of the Project the Instance group is associated with.
     */
    projectId?: pulumi.Input<string>;
    /**
     * The tags associated with the Instance group.
     */
    tags?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The ID of the Instance template to attach to the Instance group.
     */
    templateId: pulumi.Input<string>;
    /**
     * `zone`) The zone in which the Instance group exists.
     */
    zone?: pulumi.Input<string>;
}
