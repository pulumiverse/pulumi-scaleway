// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Gets information about an Edge Services route stage.
 *
 * A route stage defines HTTP request routing rules that forward requests to different backend stages based on method and path matching.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Retrieve an Edge Services route stage by its ID
 * const byId = scaleway.edgeservices.getRouteStage({
 *     routeStageId: "11111111-1111-1111-1111-111111111111",
 * });
 * ```
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Retrieve an Edge Services route stage by pipeline ID
 * const byPipeline = scaleway.edgeservices.getRouteStage({
 *     pipelineId: main.id,
 * });
 * ```
 */
export function getRouteStage(args?: GetRouteStageArgs, opts?: pulumi.InvokeOptions): Promise<GetRouteStageResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("scaleway:edgeservices/getRouteStage:getRouteStage", {
        "pipelineId": args.pipelineId,
        "routeStageId": args.routeStageId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRouteStage.
 */
export interface GetRouteStageArgs {
    /**
     * The ID of the pipeline.
     */
    pipelineId?: string;
    /**
     * The ID of the route stage. Conflicts with all filter arguments below.
     *
     * The following filter arguments are supported (cannot be used with `routeStageId`):
     */
    routeStageId?: string;
}

/**
 * A collection of values returned by getRouteStage.
 */
export interface GetRouteStageResult {
    readonly backendStageId: string;
    readonly createdAt: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly pipelineId?: string;
    readonly projectId: string;
    readonly routeStageId?: string;
    readonly rules: outputs.edgeservices.GetRouteStageRule[];
    readonly updatedAt: string;
    readonly wafStageId: string;
}
/**
 * Gets information about an Edge Services route stage.
 *
 * A route stage defines HTTP request routing rules that forward requests to different backend stages based on method and path matching.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Retrieve an Edge Services route stage by its ID
 * const byId = scaleway.edgeservices.getRouteStage({
 *     routeStageId: "11111111-1111-1111-1111-111111111111",
 * });
 * ```
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Retrieve an Edge Services route stage by pipeline ID
 * const byPipeline = scaleway.edgeservices.getRouteStage({
 *     pipelineId: main.id,
 * });
 * ```
 */
export function getRouteStageOutput(args?: GetRouteStageOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRouteStageResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("scaleway:edgeservices/getRouteStage:getRouteStage", {
        "pipelineId": args.pipelineId,
        "routeStageId": args.routeStageId,
    }, opts);
}

/**
 * A collection of arguments for invoking getRouteStage.
 */
export interface GetRouteStageOutputArgs {
    /**
     * The ID of the pipeline.
     */
    pipelineId?: pulumi.Input<string>;
    /**
     * The ID of the route stage. Conflicts with all filter arguments below.
     *
     * The following filter arguments are supported (cannot be used with `routeStageId`):
     */
    routeStageId?: pulumi.Input<string>;
}
