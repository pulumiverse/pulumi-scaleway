// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * The `scaleway.kafka.getVersion` data source is used to retrieve information about an available Kafka version.
 *
 * Refer to the [Kafka documentation](https://www.scaleway.com/en/docs/clusters-for-kafka/) and [API documentation](https://www.scaleway.com/en/developers/api/clusters-for-kafka) for more information.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Use the latest version
 * const latest = scaleway.kafka.getVersion({
 *     name: "latest",
 * });
 * ```
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Use a specific version
 * const byName = scaleway.kafka.getVersion({
 *     name: "4.0.0",
 * });
 * ```
 */
export function getVersion(args: GetVersionArgs, opts?: pulumi.InvokeOptions): Promise<GetVersionResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("scaleway:kafka/getVersion:getVersion", {
        "name": args.name,
        "region": args.region,
    }, opts);
}

/**
 * A collection of arguments for invoking getVersion.
 */
export interface GetVersionArgs {
    /**
     * The Kafka version name. Use `latest` to retrieve the most recent available version.
     */
    name: string;
    /**
     * The region the Kafka version is available in.
     */
    region?: string;
}

/**
 * A collection of values returned by getVersion.
 */
export interface GetVersionResult {
    /**
     * The cluster configuration settings available for clusters running this version.
     */
    readonly availableSettings: outputs.kafka.GetVersionAvailableSetting[];
    /**
     * The end-of-life date of the version (RFC 3339 format).
     */
    readonly endOfLifeAt: string;
    /**
     * The ID of the version, in the `{region}/{version}` format.
     */
    readonly id: string;
    /**
     * The Kafka version name. Use `latest` to retrieve the most recent available version.
     */
    readonly name: string;
    /**
     * The region the Kafka version is available in.
     */
    readonly region: string;
}
/**
 * The `scaleway.kafka.getVersion` data source is used to retrieve information about an available Kafka version.
 *
 * Refer to the [Kafka documentation](https://www.scaleway.com/en/docs/clusters-for-kafka/) and [API documentation](https://www.scaleway.com/en/developers/api/clusters-for-kafka) for more information.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Use the latest version
 * const latest = scaleway.kafka.getVersion({
 *     name: "latest",
 * });
 * ```
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as scaleway from "@pulumiverse/scaleway";
 *
 * // Use a specific version
 * const byName = scaleway.kafka.getVersion({
 *     name: "4.0.0",
 * });
 * ```
 */
export function getVersionOutput(args: GetVersionOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetVersionResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("scaleway:kafka/getVersion:getVersion", {
        "name": args.name,
        "region": args.region,
    }, opts);
}

/**
 * A collection of arguments for invoking getVersion.
 */
export interface GetVersionOutputArgs {
    /**
     * The Kafka version name. Use `latest` to retrieve the most recent available version.
     */
    name: pulumi.Input<string>;
    /**
     * The region the Kafka version is available in.
     */
    region?: pulumi.Input<string | undefined>;
}
