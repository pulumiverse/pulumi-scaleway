# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities
from . import outputs

__all__ = [
    'GetConfigResult',
    'AwaitableGetConfigResult',
    'get_config',
    'get_config_output',
]

@pulumi.output_type
class GetConfigResult:
    """
    A collection of values returned by getConfig.
    """
    def __init__(__self__, custom_logs_retentions=None, custom_metrics_retentions=None, custom_traces_retentions=None, id=None, product_logs_retentions=None, product_metrics_retentions=None, region=None):
        if custom_logs_retentions and not isinstance(custom_logs_retentions, list):
            raise TypeError("Expected argument 'custom_logs_retentions' to be a list")
        pulumi.set(__self__, "custom_logs_retentions", custom_logs_retentions)
        if custom_metrics_retentions and not isinstance(custom_metrics_retentions, list):
            raise TypeError("Expected argument 'custom_metrics_retentions' to be a list")
        pulumi.set(__self__, "custom_metrics_retentions", custom_metrics_retentions)
        if custom_traces_retentions and not isinstance(custom_traces_retentions, list):
            raise TypeError("Expected argument 'custom_traces_retentions' to be a list")
        pulumi.set(__self__, "custom_traces_retentions", custom_traces_retentions)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if product_logs_retentions and not isinstance(product_logs_retentions, list):
            raise TypeError("Expected argument 'product_logs_retentions' to be a list")
        pulumi.set(__self__, "product_logs_retentions", product_logs_retentions)
        if product_metrics_retentions and not isinstance(product_metrics_retentions, list):
            raise TypeError("Expected argument 'product_metrics_retentions' to be a list")
        pulumi.set(__self__, "product_metrics_retentions", product_metrics_retentions)
        if region and not isinstance(region, str):
            raise TypeError("Expected argument 'region' to be a str")
        pulumi.set(__self__, "region", region)

    @_builtins.property
    @pulumi.getter(name="customLogsRetentions")
    def custom_logs_retentions(self) -> Sequence['outputs.GetConfigCustomLogsRetentionResult']:
        """
        Retention limits and default for custom logs data sources.
        """
        return pulumi.get(self, "custom_logs_retentions")

    @_builtins.property
    @pulumi.getter(name="customMetricsRetentions")
    def custom_metrics_retentions(self) -> Sequence['outputs.GetConfigCustomMetricsRetentionResult']:
        """
        Retention limits and default for custom metrics data sources.
        """
        return pulumi.get(self, "custom_metrics_retentions")

    @_builtins.property
    @pulumi.getter(name="customTracesRetentions")
    def custom_traces_retentions(self) -> Sequence['outputs.GetConfigCustomTracesRetentionResult']:
        """
        Retention limits and default for custom traces data sources.
        """
        return pulumi.get(self, "custom_traces_retentions")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="productLogsRetentions")
    def product_logs_retentions(self) -> Sequence['outputs.GetConfigProductLogsRetentionResult']:
        """
        Retention limits and default for Scaleway product logs data sources.
        """
        return pulumi.get(self, "product_logs_retentions")

    @_builtins.property
    @pulumi.getter(name="productMetricsRetentions")
    def product_metrics_retentions(self) -> Sequence['outputs.GetConfigProductMetricsRetentionResult']:
        """
        Retention limits and default for Scaleway product metrics data sources.
        """
        return pulumi.get(self, "product_metrics_retentions")

    @_builtins.property
    @pulumi.getter
    def region(self) -> Optional[_builtins.str]:
        return pulumi.get(self, "region")


class AwaitableGetConfigResult(GetConfigResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetConfigResult(
            custom_logs_retentions=self.custom_logs_retentions,
            custom_metrics_retentions=self.custom_metrics_retentions,
            custom_traces_retentions=self.custom_traces_retentions,
            id=self.id,
            product_logs_retentions=self.product_logs_retentions,
            product_metrics_retentions=self.product_metrics_retentions,
            region=self.region)


def get_config(region: Optional[_builtins.str] = None,
               opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetConfigResult:
    """
    Gets regional Cockpit configuration, including retention limits and defaults for custom and product data sources.

    Use this data source to validate `retention_days` values before creating or updating `observability.Source` resources.

    Refer to Cockpit's [product documentation](https://www.scaleway.com/en/docs/observability/cockpit/concepts/) and [API documentation](https://www.scaleway.com/en/developers/api/cockpit/regional-api) for more information.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_scaleway as scaleway
    import pulumiverse_scaleway as scaleway

    main = scaleway.observability.get_config(region="fr-par")
    metrics = scaleway.observability.Source("metrics",
        name="my-metrics",
        type="metrics",
        retention_days=main.custom_metrics_retentions[0].default_days)
    pulumi.export("customMetricsRetentionBounds", main.custom_metrics_retentions[0])
    ```


    :param _builtins.str region: The region to query. Defaults to the region configured in the provider.
    """
    __args__ = dict()
    __args__['region'] = region
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('scaleway:observability/getConfig:getConfig', __args__, opts=opts, typ=GetConfigResult).value

    return AwaitableGetConfigResult(
        custom_logs_retentions=pulumi.get(__ret__, 'custom_logs_retentions'),
        custom_metrics_retentions=pulumi.get(__ret__, 'custom_metrics_retentions'),
        custom_traces_retentions=pulumi.get(__ret__, 'custom_traces_retentions'),
        id=pulumi.get(__ret__, 'id'),
        product_logs_retentions=pulumi.get(__ret__, 'product_logs_retentions'),
        product_metrics_retentions=pulumi.get(__ret__, 'product_metrics_retentions'),
        region=pulumi.get(__ret__, 'region'))
def get_config_output(region: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetConfigResult]:
    """
    Gets regional Cockpit configuration, including retention limits and defaults for custom and product data sources.

    Use this data source to validate `retention_days` values before creating or updating `observability.Source` resources.

    Refer to Cockpit's [product documentation](https://www.scaleway.com/en/docs/observability/cockpit/concepts/) and [API documentation](https://www.scaleway.com/en/developers/api/cockpit/regional-api) for more information.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_scaleway as scaleway
    import pulumiverse_scaleway as scaleway

    main = scaleway.observability.get_config(region="fr-par")
    metrics = scaleway.observability.Source("metrics",
        name="my-metrics",
        type="metrics",
        retention_days=main.custom_metrics_retentions[0].default_days)
    pulumi.export("customMetricsRetentionBounds", main.custom_metrics_retentions[0])
    ```


    :param _builtins.str region: The region to query. Defaults to the region configured in the provider.
    """
    __args__ = dict()
    __args__['region'] = region
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('scaleway:observability/getConfig:getConfig', __args__, opts=opts, typ=GetConfigResult)
    return __ret__.apply(lambda __response__: GetConfigResult(
        custom_logs_retentions=pulumi.get(__response__, 'custom_logs_retentions'),
        custom_metrics_retentions=pulumi.get(__response__, 'custom_metrics_retentions'),
        custom_traces_retentions=pulumi.get(__response__, 'custom_traces_retentions'),
        id=pulumi.get(__response__, 'id'),
        product_logs_retentions=pulumi.get(__response__, 'product_logs_retentions'),
        product_metrics_retentions=pulumi.get(__response__, 'product_metrics_retentions'),
        region=pulumi.get(__response__, 'region')))
